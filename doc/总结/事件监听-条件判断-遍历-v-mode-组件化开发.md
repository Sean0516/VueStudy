###  事件监听
#### 参数问题
- btnClick
- btnClick(event)
- btnClick(abc,event) -$event
#### 修饰符
- .stop  
- .prevent
-.enter
- .once
- .native 
  
### 条件判断
- v-if v-else-if / v-else
- v-show 和v-if  区别
### 循环遍历
- 遍历数组
        item in array
-遍历数组
    .  value
    .  value,key
    .  value,key, index
### .v-model 的使用
 - v-model -->  v-bind:value v-on:input 
 - 修饰符
    .lazy
    . number
    . trim
### 组件化开发
    1. 认识组件化
    2. 组件的基础使用
    3. 全局组件和局部组件
    4. 父组件和子组件
    5. 模板的分类写法
        . template
    6. 数据的存放
        . 子组件不能直接访问父组件
        . 子组件中有自己的data ,而且必须是一个函数
        . 为什么必须是一个函数
    7. 父子组件的通信
        . 父传子  props
        . 子传父  $emit()
    8. 父子传值 parant root chilred refs 



